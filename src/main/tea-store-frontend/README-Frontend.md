# To run the vite/react/ts app run
`npm run dev`


# Steps to create this project were:

`npm create vite@latest tea-store-frontend --template react-ts`

`/cd to folder`

`npm install`

`npm run dev`
